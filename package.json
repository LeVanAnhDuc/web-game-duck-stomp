{
  "name": "web-game-platformer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A 2D pixel platformer that runs in the browser. No install, no account, exactly two controls.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:core": "node scripts/check-core-boundary.mjs",
    "check": "npm run lint:core && npm run test && npm run build"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
